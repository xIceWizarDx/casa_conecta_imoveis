return (        <DevErrorBoundary><div className="old-site">            <Head title="Painel" />            <PainelHeader />            <div className="container-responsive section-spacing flex flex-col gap-6 pt-6">                {notice && (                    <Alert variant={notice.type === 'error' ? 'destructive' : 'default'}>                        <AlertTitle>{notice.title}</AlertTitle>                        {notice.message && <AlertDescription>{notice.message}</AlertDescription>}                    </Alert>                )}                {/* Tabs */}                <div className="mb-4 border-b">                    <div className="flex gap-2">                        <button                            type="button"                            onClick={() => setTab('publicacoes')}                            className={`-mb-px border-b-2 px-4 py-2 ${tab === 'publicacoes' ? 'border-primary text-primary' : 'border-transparent text-muted-foreground hover:text-foreground'}`}                        >                            Publicações                        </button>                        <button                            type="button"                            onClick={() => setTab('images')}                            className={`-mb-px border-b-2 px-4 py-2 ${tab === 'images' ? 'border-primary text-primary' : 'border-transparent text-muted-foreground hover:text-foreground'}`}                        >                            Imagens                        </button>                    </div>                </div>                {/* Imagens */}                {tab === 'images' && (                    <Card>                        <CardHeader>                            <CardTitle>Gerenciar Imagens</CardTitle>                        </CardHeader>                        <CardContent className="space-y-4">                            <div className="flex flex-col gap-2 sm:flex-row sm:items-end">                                <div className="flex-1">                                    <Label htmlFor="images">Enviar imagens (até 20 por vez)</Label>                                    <Input id="images" type="file" multiple accept="image/*" onChange={(e) => setFiles(e.target.files)} />                                </div>                                <div>                                    <Button onClick={onUpload} disabled={isUploading || !files || files.length === 0}>                                        {isUploading ? 'Enviando…' : 'Enviar'}                                    </Button>                                </div>                            </div>                            {images.length > 0 ? (                                <div className="grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6">                                    {images.map((img) => (                                        <div key={img.id} className="group relative overflow-hidden rounded-md border">                                            <img src={img.url} alt={img.original_name} className="h-36 w-full object-cover" />                                            <div className="absolute inset-x-0 bottom-0 flex items-center justify-between bg-black/50 p-1 text-xs text-white opacity-0 transition group-hover:opacity-100">                                                <span className="truncate" title={img.original_name}>                                                    {img.original_name}                                                </span>                                                <button className="rounded bg-red-600 px-2 py-0.5 text-[11px]" onClick={() => deleteImage(img.id)}>                                                    Excluir                                                </button>                                            </div>                                        </div>                                    ))}                                </div>                            ) : (                                <p className="text-sm text-muted-foreground">Nenhuma imagem enviada ainda.</p>                            )}                        </CardContent>                    </Card>                )}                {/* Hero Slides */}                {tab === 'publicacoes' && (                    <>                        {/* Seção: Hero Slides */}                        <div className="mb-2 flex flex-wrap items-center justify-between gap-2">                            <div className="flex items-center gap-2">                                <h2 className="text-lg font-semibold">Hero Slides</h2>                                <Button size="icon" aria-label="Adicionar Slide" onClick={() => setHeroModalOpen(true)}>                                    <Plus />                                </Button>                            </div>                            {/* Botão de atualizar removido */}                        </div>                        <div className="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">                            {slides.map((s) => (                                <SlideCard key={s.id} slide={s} onEdit={editSlide} onDelete={deleteSlide} />                            ))}                        </div>                        {/* Seção: Imóveis em Destaque */}                        <div className="mt-10 mb-2 flex flex-wrap items-center justify-between gap-2">                            <div className="flex items-center gap-2">                                <h2 className="text-lg font-semibold">Imóveis em Destaque</h2>                                <Button size="icon" aria-label="Adicionar Destaque" onClick={() => setFeaturedModalOpen(true)}>                                    <Plus />                                </Button>                            </div>                            {/* Botão de atualizar removido */}                        </div>                        <div className="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">                            {featured.map((f) => (                                <div key={f.id} className="property-card bg-white rounded-2xl shadow-lg overflow-hidden">                                    <div className="relative">                                        <div className="absolute right-2 top-2 z-10">                                            <DropdownMenu>                                                <DropdownMenuTrigger asChild>                                                    <Button size="icon" variant="secondary" aria-label="Mais ações">                                                        <MoreVertical />                                                    </Button>                                                </DropdownMenuTrigger>                                                <DropdownMenuContent align="end">                                                    <DropdownMenuItem onClick={() => editFeatured(f)}>Editar</DropdownMenuItem>                                                    <DropdownMenuItem onClick={() => deleteFeatured(f.id)}>Apagar</DropdownMenuItem>                                                </DropdownMenuContent>                                            </DropdownMenu>                                        </div>                                        <div className="h-64 overflow-hidden">                                            {f.image_url ? (                                                <img                                                    src={f.image_url}                                                    alt={f.title}                                                    className="h-full w-full object-cover transition-transform duration-300 hover:scale-105"                                                />                                            ) : (                                                <div className="h-full w-full bg-muted" />                                       
